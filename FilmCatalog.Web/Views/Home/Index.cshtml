@{
    ViewData["Title"] = "Головна";
}

<div class="jumbotron bg-light p-5 rounded-3 mb-4">
    <h1 class="display-4">🎬 Каталог фільмів</h1>
    <p class="lead">Ласкаво просимо до каталогу фільмів! Тут ви можете переглядати, додавати та редагувати інформацію про фільми, жанри та режисерів.</p>
    <hr class="my-4" />
    <div class="row text-center">
        <div class="col-md-4">
            <div class="card bg-primary text-white mb-3">
                <div class="card-body">
                    <h2 class="card-title" id="films-count">@ViewBag.FilmsCount</h2>
                    <p class="card-text">Фільмів</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-success text-white mb-3">
                <div class="card-body">
                    <h2 class="card-title" id="genres-count">@ViewBag.GenresCount</h2>
                    <p class="card-text">Жанрів</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-info text-white mb-3">
                <div class="card-body">
                    <h2 class="card-title" id="directors-count">@ViewBag.DirectorsCount</h2>
                    <p class="card-text">Режисерів</p>
                </div>
            </div>
        </div>
    </div>
</div>

<h2>Топ фільмів за рейтингом</h2>
<div class="table-responsive">
    <table class="table table-striped table-hover" id="top-films-table">
        <thead class="table-dark">
            <tr>
                <th>Назва</th>
                <th>Рік</th>
                <th>Жанр</th>
                <th>Режисер</th>
                <th>Рейтинг</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var film in ViewBag.TopFilms)
            {
                <tr>
                    <td>
                        <a asp-controller="Films" asp-action="Details" asp-route-id="@film.Id">@film.Title</a>
                    </td>
                    <td>@film.Year</td>
                    <td>@film.Genre?.Name</td>
                    <td>@film.Director?.Name</td>
                    <td>
                        @if (film.Rating != null)
                        {
                            <span class="badge bg-warning text-dark">⭐ @(((decimal)film.Rating).ToString("F1"))</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div class="row mt-4">
    <div class="col-md-4">
        <a asp-controller="Films" asp-action="Index" class="btn btn-primary btn-lg w-100">
            Переглянути всі фільми
        </a>
    </div>
    <div class="col-md-4">
        <a asp-controller="Films" asp-action="Create" class="btn btn-success btn-lg w-100">
            Додати новий фільм
        </a>
    </div>
    <div class="col-md-4">
        <a asp-controller="Genres" asp-action="Index" class="btn btn-info btn-lg w-100">
            Переглянути жанри
        </a>
    </div>
</div>
